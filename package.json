{
  "name": "otp-fast2sms-integration",
  "private": true,
  "version": "0.1.0",
  "type": "commonjs",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "dev": "vercel dev",
    "lint": "node -c api/request-otp.js && node -c api/verify-otp.js && node -c api/health.js && node -c api/_lib/mongo.js"
  },
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "mongodb": "^6.7.0"
  },
  "devDependencies": {
    "vercel": "^34.3.0"
  }
}
